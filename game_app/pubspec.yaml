name: pascalingo
description: A language learning game.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.0.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/

  # Pour le web, pas besoin de configuration supplémentaire

dev_dependencies:
  flutter_test:
    sdk: flutter